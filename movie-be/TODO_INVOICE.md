# TODO_INVOICE

## Mục tiêu: format invoice date theo yyyy-MM-dd

- [x] Cập nhật `InvoiceDTO.bookingDate` trả về dạng `String yyyy-MM-dd` (entity `LocalDateTime`).
- [x] Cập nhật `InvoiceDTO.scheduleShow` trả về dạng `String yyyy-MM-dd` (entity `LocalDate`).
- [x] Cập nhật `InvoiceServiceImpl` parse/validate và format theo `yyyy-MM-dd`.
- [ ] Chạy build/test để đảm bảo không lỗi compile.



